---
id: coverage-gates
title: 覆盖率门禁（跨项目复用）
order: 60
tags: [coverage, gates, cross-project]
rules:
  - id: coverage-gates.personal-python
    title: 个人 Python 项目覆盖率门禁
    keywords: ["个人Python", "setup-coverage-gate", "coverage-gate-py", "coverage gate", "覆盖率门禁"]
  - id: coverage-gates.corp-multilang
    title: 公司多语言项目覆盖率门禁
    keywords: ["公司项目", "多语言", "coverage-gate-corp", "JaCoCo", "jest"]
---

## 覆盖率门禁（跨项目复用）

两套独立方案，按项目类型选用：

### 个人 Python 项目

Skill：`/setup-coverage-gate`，模板在 `/Users/zhiweiyin/repo_ds1600/coverage-gate-py/`

在任何 Python 项目中调用，自动生成 `scripts/run_coverage.py`（--gate 门禁）+ `docs/testing_policy.md` + 更新 CLAUDE.md 交付门禁。分层目标原则：Models ≥ 90-95%，Services ≥ 80%，API ≥ 70%，Utils ≥ 85%，整体 ≥ 75-95%。

已落地项目：`article_archieve_claude`、`llm-benchmark-claude`。

### 公司多语言项目

策略和工具集在 `/Users/zhiweiyin/repo_ds1600/coverage-gate-corp/`

支持 Java（JaCoCo）、Go（go test -cover）、JS（jest）。统一 JSON 报告格式 + 多服务仪表盘。策略核心：先量不卡 → 增量门禁 → 渐进全量，反"凑数"陷阱，ROI 6 指标可量化追踪。

### 个人 Bash 项目

无标准覆盖率工具（bash 无 line coverage），采用函数覆盖率 + 一致性检查替代。

分层目标：

| 维度 | 目标 | 说明 |
|------|------|------|
| 函数覆盖率 | ≥ 80% | 已定义函数中被测试直接调用的比例 |
| CLI 参数 | 100% | 每个命令行参数至少有 1 个测试用例 |
| 数据一致性 | 0 FAIL | C1-C13 一致性检查全部 PASS 或合理 SKIP |
| 关键错误路径 | 100% | 非 git 目录、参数越界、损坏缓存等必须有测试 |
| 上限 | ≤ 90% | Google 2020：超过 90% "most likely not worth it" |

Per-commit gate：`run_tests.sh 1` 全 PASS，无 FAIL。
