# 开发日志：2026-05-24

> 13 commits, 9 files changed
> 范围：`d88b03b` → `c80ee43`

## 今日交付

### 1. 数据正确性修复

**confidence 分布计数（`0f5a356`）：** `getConfidenceDistribution` 窗口函数使用 `COUNT(*) OVER (PARTITION BY confidence)` 统计的是 GROUP BY 结果行数（唯一 rule_id 数），而非引用总数。改为 `SUM(COUNT(*)) OVER` 后，113 → 8761，与实际引用数一致。

**avg_citations 计算（`c698e45`）：** `getFilteredSessions` 的 `avg_citations` 通过 `LEFT JOIN (SELECT ... GROUP BY) sub` 计算，AVG 会跳过 NULL 行（无引用的 session），导致均值偏高（8.61 vs 正确的 5.37）。合并 total + stats 查询时改用相关子查询，正确包含零引用 session。

### 2. 查询性能优化

**getRecentSessions（`d88b03b`）：** 全表 `LEFT JOIN rule_references` + `GROUP BY`（1633 sessions × 8775 references），耗时 2300ms。改为先查 N 个 session ID，再按 `idx_refs_session` 索引逐个查统计。结果：2300ms → 1ms。

**getFilteredSessions sessions 查询（`646de73`）：** 同样的全表 JOIN 模式，改为相关子查询排序 + 逐 session 索引查询。time sort: 105ms → 71ms，citations sort: 88ms → 43ms。

**getFilteredSessions 聚合查询（`c698e45`）：** 2 个查询（COUNT total + LEFT JOIN stats）合并为 1 个相关子查询。减少一次全表扫描。

**CitationHeatmap 组件（`5815d89`）：** useMemo 中已构建 `ruleMeta` 映射但未解构，导致每行用 `data.find()` 做 O(n) 查找。改为直接使用预构建的 map。

### 3. 测试加强

- 精确 confidence distribution 断言：medium=4, total=4；多级别场景 high=1, low=2, medium=4, total=7
- `getRecentSessions` duration_sec 计算：正常路径 300s，null ended_at 路径 0
- `getFilteredSessions` avg_citations 包含零引用 session 断言，avg_duration 时间戳断言，无时间戳返回 null
- Python 侧 schema 迁移测试：confidence/source/last_offset 列自动添加

测试总数：160 Python + 172 TypeScript = 332

### 4. 代码清理

- 移除误跟踪的 `.next/trace` 和 `.next/trace-build`，根 `.gitignore` 增加 `.next/`
- 消除 `db.ts` 中的重复 `RecentCitation` 接口，统一使用 `types.ts` 导出

### 5. 安全修复

**export-report.py SQL 参数化（`44f5a00`）：** `scripts/export-report.py` 中 6 个查询通过 f-string 拼接 `cutoff` 日期到 SQL 字符串。虽然输入来自内部 `date_range()` 函数（格式固定为 `%Y-%m-%d`），但仍违反参数化查询原则。改为 `?` 占位符 + 参数元组。

### 6. CI 增强（`c80ee43`）

- 新增 `test-dashboard` job：运行 `npm test`（vitest），在 build 前验证
- 升级 Node.js 20 → 22（LTS）
- `build-dashboard` 改为依赖 `test-dashboard` 通过
- CI 三 job 全绿：test-python / test-dashboard / build-dashboard

## 优化模式总结

本次核心优化模式：**全表 JOIN + GROUP BY → 相关子查询排序 + 逐行索引查询**。

适用条件：
1. 需要从大表（rule_references）聚合统计到小表（sessions/rules_metadata）
2. 只需返回有限行数（有 LIMIT）
3. 有索引支持按外键查找（idx_refs_session, idx_refs_rule）

公式：
```
-- 优化前
SELECT s.*, COUNT(r.id) FROM sessions s LEFT JOIN refs r ON ... GROUP BY s.id LIMIT N

-- 优化后
SELECT s.* FROM sessions s WHERE ... ORDER BY (subquery) LIMIT N;
-- 然后逐行: SELECT COUNT(*) FROM refs WHERE session_id = ?
```

---

## 午后代码质量审计

> 19 commits, `c80ee43` → `e53755b`
> 范围：安全修复、数据质量、健壮性、可访问性、依赖更新

### 7. 安全修复（续）

**SQL 通配符注入（`15ce759`）：** `getFilteredSessions` 的 `LIKE '%search%'` 未转义 `%` 和 `_`，搜索 "100%" 会匹配任何含 "100" 加一个字符的文本。改为 `search.replace(/[%_]/g, '\\$&')` + `ESCAPE '\\'` 子句。

**parseInt NaN 传播（`c9e3ac`）：** `parseInt('abc')` 返回 NaN，`Math.min(Math.max(NaN, 1), 200)` 返回 NaN 而非 1。添加 `Number.isFinite` 检查。

### 8. 会话元数据提取

**extract_session_metadata（`d6bb97e`）：** session-logger.py 新增函数，扫描 JSONL 前 50 行提取 `entry.message.model`（assistant 消息）和首个 user 消息文本（截断 200 字符）。`main()` 调用时传入 `upsert_session(conn, session_id, model=..., summary=...)`。

**"user" 类型修复（`e53755b`）：** JSONL 使用 `type="user"`（非 `"human"`），导致 task_summary 始终为 NULL。改为匹配两者。

**--backfill 模式（`0c1408a`）：** 一次性扫描历史会话 JSONL 填充 model 和 task_summary。`--all` 标志可重新处理 summary 为 NULL 的会话。回填结果：model 10→363 (21%)，summary 0→362 (21%)。

### 9. 前端健壮性

**除零保护（`48e95c1`）：** 3 处 `citation_trend.reduce(...)/citation_trend.length` 在空数组时产生 NaN。改为 `/(length || 1)`。

**useEffect 清理（`b2b4b80`）：** editor 规则列表 fetch 添加 `cancelled` 标志，防止 unmount 后 setState。`fetchPublishHistory` 空 catch 改为 `console.warn`。

**fetchJson 错误信息（`65a31bc`）：** 从 `"HTTP 500"` 改为尝试解析服务端 JSON body 中的 error 字段，提供 `"HTTP 500: database locked"` 等更具诊断价值的消息。

**RuleRow memo 化（`b2b4b80`）：** 规则列表行组件用 `React.memo()` 包裹，避免父组件状态变化时不必要的重渲染。

### 10. 可访问性

- Refresh 按钮：添加 `aria-label`（`48e95c1`）
- Session ID 复制：`<span>` 改为 `role="button" tabIndex={0}` + `onKeyDown` 处理 Enter/Space
- 新增 i18n key：`session.copyId`、5 个 `accessibility.*` key

### 11. 测试与覆盖

- Python：160→170 tests（+backfill 2, +user type 1），97% coverage
- Web：172→173 tests，96.7% stmts / 99.8% lines
- TypeScript strict mode：0 errors | ESLint：0 warnings

### 12. 依赖与文档

**npm 更新（`6060fd9`）：** @base-ui 1.4→1.5, @types/react, codemirror, vitest 4.1.6→4.1.7, shadcn 4.7→4.8。跳过 major bumps。

**CLAUDE.md 更新（`988a304`）：** 架构图增加 PostToolUse+Stop hooks、增量扫描说明。已知限制移除过时的"扫描整个 JSONL"条目，新增历史会话元数据说明。

### 数据质量改进

| 指标 | 改进前 | 改进后 |
|------|--------|--------|
| Sessions with model | 10 (0.6%) | 363 (22%) |
| Sessions with summary | 0 (0%) | 362 (22%) |
| Model distribution | N/A | glm-4.7: 345, glm-5.1: 6, glm-4.5-air: 9 |

---

## 晚间关键词优化与测试修复

> 2 commits, `165dc6d` → `a2fc2db`

### 13. 关键词优化（`2d9fc40`）

分析发现 38 个从未被 hook 匹配的关键词。根因：hook 做 substring 匹配，部分关键词是复合短语（如 "CLAUDE.md声明"、"命令拼接"、"造假"），在日常对话中很少出现。

修复策略：在 12 个规则文件中添加更短、更自然的同义关键词。如：
- "coverage gate" + "覆盖率门禁"（覆盖率门禁规则）
- "fabricate" + "fake output"（禁止虚构规则）
- "injection" + "注入"（安全护栏）
- "concurrency" + "同时运行"（模型并发限制）
- "diff"（手术刀原则 trace-to-request）

优化后 DB 中所有 keywords 均已有匹配记录。

### 14. Vitest Pool 修复（`a2fc2db`）

vitest.config.ts 的 `pool: 'threads'` 在 vitest 4.1.7 下无法 resolve jsdom，导致 4 个 jsdom 环境测试全部报错（ERR_MODULE_NOT_FOUND）。改回 `pool: 'forks'` 后 222 个测试全部通过。

### 测试覆盖（最终）

| Layer | Tests | Stmts | Branch | Lines |
|-------|-------|-------|--------|-------|
| Python (build, hooks, MCP) | 183 | 98% | — | — |
| Web (lib, utils, hooks, i18n) | 222 | 99.81% | 94.41% | 100% |
| **Total** | **405** | — | — | — |

### 15. ESLint 修复（`76a39ac`）

3 个文件修复：`require('os')` 改 ES import、移除未使用的 `vi` import。ESLint 从 1 error + 2 warnings 降到 0/0。

### 16. README 更新（`53b2970`）

README 落后于项目实际功能。补充：Stop Hook 配置、backfill 命令、Session/Editor 页面、置信度系统、热力图、CSV 导出、i18n、测试统计（405 tests）。中英文同步更新。

### 17. 部署脚本（`c953f54`）

新增 `scripts/setup-launchd.sh`：自动检测 arm64 homebrew Node，生成 launchd plist，加载服务。修复了原 plist 使用 `/usr/local/bin/npx`（x86_64 node 25.1.0）的问题，改为 `/opt/homebrew/bin/npx`（arm64 node 25.8.2）。

### 18. MCP Server 配置

创建项目级 `.mcp.json`，将 MCP server 注册为 stdio transport。使 `save_draft`、`validate_drafts`、`publish_drafts` 等工具在 Claude Code 中可用。已加入 `.gitignore`（含用户特定路径）。

### 数据质量分析

| 指标 | 值 |
|------|-----|
| 7 天内零引用率 | 7% (4/54 sessions on 5/24) |
| 零引用原因 | 测试 session、memory agent 子 session、历史 session |
| 孤立 session 修复 | 1 个（af4281f5） |
| 所有关键词匹配状态 | 54/54 rules 全部有匹配 |

### 最终状态

- Python 183 tests / Web 222 tests = **405 total**，全绿
- CI 3/3 jobs passed，TypeScript 0 errors，ESLint 0 warnings
- 生产环境 arm64 Node 运行正常，1688 sessions / 9499 citations
- 项目达到成熟稳定状态，设计文档 4 个阶段全部完成
