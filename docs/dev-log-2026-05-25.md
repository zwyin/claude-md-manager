# 开发日志：2026-05-25

> 10 commits
> 范围：`13d5836` → `94143bd`

## 今日交付

### 1. 健康监控基础设施

**健康端点 (`ac0839b`)：** 新增 `GET /api/health`，返回 `{status, sessions, response_ms}`。1ms 响应时间，用于生产监控。健康端点复用 `getTotalSessionCount` 共享函数 (`1ddc30e`)。

**日志轮转 (`ac0839b`)：** `setup-launchd.sh` 添加日志轮转逻辑，超过 500 行自动截断。

**launchd 防冲突 (`79cbc90`)：** 添加 `ThrottleInterval=5`，解决 EADDRINUSE 问题（历史日志中有 310 次）。

### 2. 全局快捷键

**⌘K 全局导航 (`927e2f6`)：** 新增 `GlobalShortcuts` 组件，在 dashboard、analytics、history 等无本地 ⌘K 的页面，按 ⌘K 跳转到 /rules 搜索页。

### 3. API 路由测试覆盖

**只读端点测试 (`30ae7a9`)：** 31 个测试覆盖 10 个只读 API 路由：health, sessions, sessions/[id], rules, rules/[id], analytics, citations, history, history/diff, editor/rules, editor/publish-history。

**写入端点测试 (`bc32d61`)：** 14 个测试覆盖验证、排序、draft CRUD、rollback 输入校验。

### 4. 前端组件全覆盖（新增 6 commits）

**Hook 测试 (`fb2d8d6`)：** 5 个测试覆盖 use-chart-theme 和 use-chart-tooltip hooks。

**组件测试第一批 (`c264e90`, `cb587d4`)：** 46 个测试覆盖 9 个组件：FontScaleProvider (8), GlobalShortcuts (7), ChartErrorBoundary (4), TermTooltip (3), PageStates (13), ShortcutHelp (8), TopBar (5), ErrorBoundary (5), BackToTop (4), ScrollRestorer (2), RoutedErrorBoundary (2)。

**组件测试第二批 (`62e6dd7`)：** 33 个测试覆盖最后 5 个组件：MetricVisualizations (10), StatCard (7), RuleRow (6), CitationHeatmap (6), AppSidebar (4)。全部 16 个组件均有测试文件。

**页面和编辑器子组件 (`ac91cda`)：** 26 个测试覆盖 Error (4), Loading (2), GlobalError (4), PreviewPanel (3), PublishDialog (8), EditorLayout (5)。

**StatCard 动画测试 (`94143bd`)：** 5 个测试用 fake timers 覆盖 rAF 动画完成后的值渲染。

### 5. 数据库性能优化

**索引优化：** 在 sessions 表添加 `idx_sessions_started` 和 `idx_sessions_model` 索引，SCAN → SEARCH，查询性能显著提升。

## 测试统计

| 层级 | 测试数 | 覆盖率 |
|------|--------|--------|
| Python (hooks, build, MCP) | 183 | 98% stmts, mcp_lib 100% |
| Web (44 test files: all 16 components, 5 editor/app pages, all hooks, all API routes, all lib) | 410 | 96.8% stmts, 91.9% branches, 97.3% funcs |
| **Total** | **593** | — |

## 数据基线

- 1708 sessions, 9652 citations, 54 rules
- Health endpoint: 1ms 响应时间
- 生产零停机（launchd 自动重启）
