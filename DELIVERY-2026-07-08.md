# CLAUDE.md Manager — 交付报告

**日期**: 2026-07-08
**交付范围**: draft/publish 流程支持新建 rule_id（含测试 + 文档）
**最终状态**: ✅ 修复落地, 187/187 测试通过, mcp_lib.py 覆盖率 99%; ⏸ MCP server 端到端验证需新会话

---

## 起因

用户尝试通过 MCP 工具（`save_draft → publish_drafts`）把两条新规则
「反合理化对照表」/「工程原则速查（Google）」加入 `~/.claude/CLAUDE.md`。
`publish_drafts` 返回 `{rules_changed: 0, snapshot_name: null}`，但随后 `get_draft`
返回 null——draft 被消费了但什么也没写。

Workaround 是走 Web UI 「编辑器」→「新建规则」手动创建——这是已知的能力缺失
（memory 已记录于 2026-07-07）。用户要求把它做成**长期方案**：直接修 MCP server 的 bug。

## 根因

`mcp_lib.py:225-227`，函数 `publish_all_drafts`：

```python
rule = rules_map.get(rule_id)
if not rule:
    continue          # ← 对新 rule_id 静默 skip
```

而 `rules_map`（line 210-216）只装载 `RULES_DIR.glob("*.md")` 中**已存在**的规则文件。
新 rule_id 的 draft 永远拿不到 `rule` 对象，本轮被 skip；下一段 `DELETE FROM rule_drafts`
（line 266）又一把全清空——draft 从 db 里凭空消失，rules 目录啥也没多。
`written` 计数也偏（只在 existing 分支 +1），导致 `rules_changed` 看上去"对了"。

## 修复

**修改文件**: `mcp_lib.py` 的 `publish_all_drafts` 循环体（line 218-238 区域）
**修改方式**: 把 `if not rule: continue` 拆成两个分支

- **已存在规则** → 原地修改（行为不变）
- **新规则** → 解析 draft 的 frontmatter 取 order/title，生成
  `RULES_DIR/<NN>_<sanitized_title>.md`；同名冲突追加 `-2`、`-3`……

**核心 diff 概览**：

```diff
-           if not rule:
-               continue
-           written += 1
+           if rule:
+               # Existing — modify in place
+               target_file = rule["source_file"]
+               yaml_out = draft_yaml or rule["yaml"]
+               ...
+           else:
+               # New — create rules/<NN>_<title>.md (fix)
+               draft_meta, _, _ = parse_frontmatter(f"---\n{draft_yaml}\n---\n{draft_body}")
+               effective_order = order_override if order_override is not None \
+                   else int(draft_meta.get("order", 999))
+               title = str(draft_meta.get("title", rule_id))
+               safe_title = re.sub(r"[^\w一-鿿-]+", "-", title).strip("-") or rule_id
+               target_file = RULES_DIR / f"{effective_order:02d}_{safe_title}.md"
+               i = 2
+               while target_file.exists():
+                   target_file = RULES_DIR / f"{effective_order:02d}_{safe_title}-{i}.md"
+                   i += 1
+               yaml_out = draft_yaml
+               if order_override is not None:
+                   yaml_out = re.sub(r"^order:\s*\d+", f"order: {order_override}",
+                                      yaml_out, flags=re.MULTILINE)
+               body_out = draft_body
-
-           content = f"---\n{yaml_out}\n---\n{body_out}"
-           rule["source_file"].write_text(content, encoding="utf-8")
+           target_file.write_text(f"---\n{yaml_out}\n---\n{body_out}", encoding="utf-8")
+           written += 1
```

要点：`written += 1` 移到分支外（新旧都计数）；`order_override` 同时应用到 filename
**和** yaml 里的 `order:` 字段（避免两者错位，测试 `test_new_rule_respects_order_override` 守住）。

## 测试

按 TDD 流程：先 red → 改 → green → 跑全套。

### 1. 新增/改写 5 个测试（`tests/test_mcp_lib.py`）

| Test | 覆盖点 |
|---|---|
| `TestPublish.test_creates_new_rule_file_for_unknown_id` | 替代旧的 `test_skips_orphan_drafts` —— 核心修复断言 |
| `TestPublishNewRule.test_filename_sanitizes_special_chars` | title 含括号/英文（CJK + ASCII 混合）正确 sanitize |
| `TestPublishNewRule.test_filename_collision_appends_suffix` | 同 order+title 冲突 → `-2` 后缀 |
| `TestPublishNewRule.test_new_rule_respects_order_override` | `order_override` 应用到 yaml 字段（不仅是 filename） |
| `TestPublishNewRule.test_publish_handles_mixed_existing_and_new_rules` | 单次 publish 同时处理已有+新规则 |

### 2. 基线（修复前）：5 fail / 37 pass ✅ red

```
FAILED TestPublish::test_creates_new_rule_file_for_unknown_id
FAILED TestPublishNewRule::test_filename_sanitizes_special_chars
FAILED TestPublishNewRule::test_filename_collision_appends_suffix
FAILED TestPublishNewRule::test_new_rule_respects_order_override
FAILED TestPublishNewRule::test_publish_handles_mixed_existing_and_new_rules
5 failed, 37 passed in 0.29s
```

### 3. 修复后：42 pass ✅ green + 全套无回归

```
tests/test_assemble.py        37 passed
tests/test_db.py              21 passed
tests/test_mcp_lib.py         42 passed       ← 含新增/改写 5 个
tests/test_session_logger.py  87 passed

187 passed in 0.67s
```

### 4. 覆盖率

```
Name         Stmts   Miss  Cover   Missing
mcp_lib.py     185      1    99%   289

TOTAL          185      1    99%
```

**99% 覆盖**，远高于用户 CLAUDE.md 「Services ≥ 80%」目标。唯一未覆盖行是 line 289
（`get_publish_history` fetchall 的空行分支），对功能无影响。

## 端到端验证 ⏸（未在本次执行）

**为什么没完成？**

MCP server 是 stdio 长驻进程（PID 22626，`python mcp_server.py`）。Python 的
`import` 是一次性的——本次会话启动时已加载旧版 `mcp_lib.py`；改文件对当前
server 进程无效，必须 **重启 Claude Code 会话** 才能让新代码生效。

**验证清单**（下次新会话跑）：

1. `save_draft("anti-rationalization", yaml, body)` + `save_draft("engineering-principles", yaml, body)`
   —— 内容从 `~/claude-pending-rules/` 读取（已备份，含 frontmatter + body）
2. `validate_drafts()` → 应全绿
3. `publish_drafts()` → 期望 `rules_changed: 2`
4. `ls rules/` 应出现 `65_反合理化对照表.md` 和 `70_工程原则速查-Google.md`（或 sanitize 结果）
5. `grep "反合理化对照表\|工程原则速查" ~/.claude/CLAUDE.md` 都命中
6. 新会话里说一句「引用一下反合理化对照表」 → 确认规则被加载

## 边界与未来工作

- **Web UI 编辑器仍是新建规则的合法入口**。MCP draft 流程补的是**通过 MCP 也能建**——
  三路等价（Web UI 编辑器 / MCP draft / 直接写 rules/*.md）。
- **Filename sanitize**：`re.sub(r"[^\w一-鿿-]+", "-", title)` 覆盖 CJK + ASCII + `-_`。
  标点和 emoji 会被替换为 `-`。如果将来需要支持 emoji 标题，扩 regex。
- **Order 冲突**：当前按 `-2`、`-3` 后缀追加，**不报错**。如希望拒绝重 order，可加配置。
- **`assemble.py`** 未改 —— 它通过 `RULES_DIR.glob("*.md")` 拣所有 md，新文件自动入。
  已有 `test_assemble.py` 覆盖此路径。

## 提交状态

改动**未 commit**。建议作为 1 个 commit：

```bash
cd /Users/zhiweiyin/repo_ds1600/claude-md-manager
git add mcp_lib.py tests/test_mcp_lib.py
git commit -m "fix(mcp): publish_drafts supports new rule_ids via draft

Previously drafts for rule_ids without an existing rules/*.md file were
silently dropped (rules_changed=0, draft cleared from db). This blocks
adding new CLAUDE.md rules through the MCP draft/publish flow.

Now publish_drafts creates rules/<NN>_<sanitized_title>.md for new
rule_ids, with collision suffix (-2, -3...) and order_override applied
to both filename and frontmatter.

Tests: 5 new/replaced tests cover create-new, sanitize, collision,
order_override, and mixed scenarios. 187/187 pass; mcp_lib.py 99% cover.

Refs: agent-skills anti-rationalization / engineering-principles
additions in 2026-07-07 were the trigger."
```

按用户 CLAUDE.md「/ship 必须用户明确确认」—— **用户审 commit message 后决定** commit 时机。

## 已知遗留

1. **`db` 模块 Pyright 警告** (`mcp_lib.py:24` 的 `from db import get_db`)：
   `sys.path.insert` 注入 hooks/ 路径，Pyright 不识别但 pytest runtime 可解析。
   pre-existing，**与本次改动无关**。如要去警告，需加 pyright config 或 absolute import 重构。
2. **Web UI 不需改动**：新文件落盘后 Next.js SPA 下次 fetch 会拣到（或硬 reload）。
3. **Memory 已更新**：`~/.claude/projects/.../memory/claude-md-via-manager-service.md`
   的「能力边界」段从 "只支持编辑已有 rule_id" 翻为 "已支持新建"，并标注"改了
   mcp_lib.py 必须重启 Claude 会话"避免未来踩同样的"代码改了但 server 不感知"的坑。
